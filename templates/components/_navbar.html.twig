<nav class="navbar navbar-expand-lg navbar-color fixed-top py-3">
    <div class="container-fluid">
        <a class="navbar-brand logo-navbar" href="{{ path('home') }}">
            <img src="{{ asset('build/images/Fiverr_Logo_GreenGreen_RGB.svg') }}" alt="logo" class="logo-navbar me-1">
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
            <i class="fas fa-bars"></i>
        </button>
        <div class="collapse navbar-collapse justify-content-end" id="navbarNavAltMarkup">
            <div class="navbar-nav">
                {% if not app.user %}
                    <li class="nav-item">
                        <a class="nav-link active py-2 text-dark" aria-current="page" href="{{ path('app_login') }}">Sign in</a>
                    </li>
                    <li class="nav-item">
                        <a class="btn btn-outline-primary px-2 py-1 mt-1 ms-2 join" aria-current="page" href="#">Join</a>
                    </li>
                {% else %}
                    <div class="dropdown">
                        <a class="nav-link dropdown-toggle text-primary" href="#" role="button" id="dropdownMenuLink" data-bs-toggle="dropdown" aria-expanded="false">
                            Bonjour
                            <span class="text-secondary">{{ app.user.username|capitalize }}</span>
                        </a>
                        <ul class="dropdown-menu shadow-lg w-100" aria-labelledby="dropdownMenuLink">
                            <li>
                                <a class="dropdown-item text-wrap text-dark py-2" href="#">
                                    <i class="fas fa-user me-2"></i>Voir mon profil</a>
                            </li>
                            {% if is_granted('ROLE_ADMIN') %}
                                <li>
                                    <a class="dropdown-item text-wrap text-warning py-2" href="#">
                                        <i class="fas fa-tools me-2"></i>Administration</a>
                                </li>
                            {% endif %}
                            <li>
                                <a class="dropdown-item text-wrap text-danger py-2" href="{{ path('app_logout') }}">
                                    <i class="fas fa-sign-out-alt me-1"></i>
                                    Déconnexion
                                </a>
                            </li>
                        </ul>
                    </div>
                {% endif %}
            </div>
        </div>
    </div>
</nav>
