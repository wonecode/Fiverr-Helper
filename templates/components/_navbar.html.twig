<nav class="navbar navbar-expand-lg navbar-color fixed-top py-3">
    <div class="container-fluid">
        <a class="navbar-brand logo-navbar" href="{{ path('home') }}">
            <img src="{{ asset('build/images/Fiverr_Logo_GreenGreen_RGB.svg') }}" alt="logo" class="logo-navbar me-1">
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
            <i class="fas fa-bars"></i>
        </button>
        <div class="collapse navbar-collapse justify-content-end" id="navbarNavAltMarkup">
            <div class="navbar-nav">
                {% if not app.user %}
                    <li class="nav-item mt-2 mt-lg-0">
                        <a class="nav-link active py-2 text-primary" aria-current="page" href="{{ path('app_login') }}">Sign in</a>
                    </li>
                {% else %}
                    <li class="nav-item mt-2 mt-lg-0">
                        <a class="nav-link active me-2 pb-0 text-muted mt-lg-0 mt-2" aria-current="page" href="{{ path('help_index') }}">Help freelancers</a>
                    </li>
                    <li class="nav-item mt-2 mt-lg-0">
                        <a class="nav-link active me-2 text-muted" aria-current="page" href="{{ path('ranking_index') }}">Leaderboard</a>
                    </li>
                    <div class="dropdown">
                        <a class="nav-link dropdown-toggle text-muted" href="#" role="button" id="dropdownMenuLink" data-bs-toggle="dropdown" aria-expanded="false">
                            Hello
                            <span class="nav-name text-primary">{{ app.user.username }}</span>
                            <span class="badge bg-primary align-middle">Level {{ app.user.level }} | {{ app.user.experience }} XP</span>
                        </a>
                        <ul class="dropdown-menu shadow-lg" aria-labelledby="dropdownMenuLink">
                            <li>
                                <a class="dropdown-item text-wrap text-dark py-2" href="{{ path('profile', {id: app.user.id}) }}">
                                    <i class="fas fa-user me-2"></i>My profile</a>
                            </li>
                            {% if is_granted('ROLE_ADMIN') %}
                                <li>
                                    <a class="dropdown-item text-wrap text-primary py-2" href="{{ path('admin') }}">
                                        <i class="fas fa-tools me-2"></i>Admin panel</a>
                                </li>
                            {% endif %}
                            <li>
                                <a class="dropdown-item text-wrap text-danger py-2" href="{{ path('app_logout') }}">
                                    <i class="fas fa-sign-out-alt me-1"></i>
                                    Sign out
                                </a>
                            </li>
                        </ul>
                    </div>
                {% endif %}
            </div>
        </div>
    </div>
</nav>
